# Project AI Context

This file provides high-level context required to work effectively in this repository.
It should remain short (≈1 page). Update it when core assumptions change.

---

## Project Overview

- Project purpose:
- Target users:
- Primary use cases:

---

## Tech Stack

- Language(s):
- Frameworks:
- Runtime:
- Database:
- ORM / DB layer:
- Auth:
- Infrastructure / hosting:

(Include exact versions if relevant.)

---

## High-Level Architecture

- Entry points (e.g. API, UI, workers):
- Core modules and responsibilities:
- Data flow (request → logic → persistence → response):

---

## Core Invariants (Never Break)

- Example: Financial data must be auditable.
- Example: All writes must be scoped to an organization.
- Example: API routes must be thin.

---

## Development Conventions

- Naming patterns:
- Folder structure expectations:
- Error handling approach:
- Logging approach:

---

## AI Usage Notes

- Prefer diffs over full files.
- Ask for missing context.
- Respect `AI_RULES.md` at all times.
